#include <bits/stdc++.h>
using namespace std;


int n ;   // n = 4
int queen[10];

int rec(int level){   //which row we are at right now .placing the value

bool cheak(int row , int col){
    for(int i = 0 ; i< row ; i++){
        int prow = i;
        int pcol = queen[i];

        if(pcol == col  || abs(prow - row )== abs(pcol == col)){
            return 0 ;
        }

    }
    return 1 ;

}
    //puring

    //base case 
    if(level == n){
        return 1;
    }

    //compute 
    int ans = 0 ;
    int(col = 0 ; col<= n ; col++){
        if (cheak(row, col)){
            //place the queen 
            queen[level]= col;
            //expolore the option
            ans+= rec(level+1);
            //revret it back
            queen[level]= -1;

        }

    }
//return 
    retrun ans ;

}

void solve(){
    cin >> n;
    memset(queen,-1 , sizeof(queen));

}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int t=1;
    // cin>>t;
    for(int i=0;i<t;i++){
        solve();
    }
}

//example of bactracking of recursion
